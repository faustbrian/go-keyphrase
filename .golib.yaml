schema_version: 1
tool_version: v1.0.4
tool_checksums_sha256: 6c1ee70e3fa7aa333d12420dc78d7740f4de78717d1979eeb8a5f70a2a124ba0
manifest:
  modules: modules.json
  packages: packages.json
evidence:
  root: ".verification"
mutation:
  root: ".verification/mutation"
  imports:
  - module: "."
    archive: ".verification/mutation/bootstrap/root.zip"
    ledger: ".verification/mutation/migration-ledger.json"
api:
  baselines:
  - module: "."
    mode: go-doc
    path: api/baseline.txt
    packages:
    - "."
    - ./bip39
    - ./keyphrasetest
    - ./passphrase
    - ./password
    - ./wordlist
    - ./wordlist/eff
operations:
- module: "."
  gate: test
  steps:
  - type: go-test
    packages: ["./wordlist/eff"]
    run: '^TestEmbeddedListsMatchPinnedMetadata$'
    timeout: 20m
  - type: go-test
    packages: ["./bip39"]
    run: '^TestPinnedOfficialListsLoad$'
    timeout: 20m
- module: "."
  gate: fuzz
  steps:
  - type: go-test
    packages: ["./password"]
    fuzz: '^FuzzAnalyzeAlphabet$'
    budget: 10s
    timeout: 2m
  - type: go-test
    packages: ["./wordlist"]
    fuzz: '^FuzzValidation$'
    budget: 10s
    timeout: 2m
  - type: go-test
    packages: ["./bip39"]
    fuzz: '^FuzzMnemonicParsing$'
    budget: 10s
    timeout: 2m
  - type: go-test
    packages: ["./passphrase"]
    fuzz: '^FuzzParsing$'
    budget: 10s
    timeout: 2m
- module: "."
  gate: docs
  steps:
  - type: go-test
    packages: ["./..."]
    run: '^Example'
    timeout: 20m
- module: "."
  gate: benchmark
  steps:
  - type: go-test
    packages: ["./..."]
    benchmark: "."
    budget: 1x
    timeout: 20m
